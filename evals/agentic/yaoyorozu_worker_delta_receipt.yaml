eval_id: yaoyorozu_worker_delta_receipt
target: agentic.yaoyorozu
level: L4
description: Repo-local Yaoyorozu worker reports must bind git-target-path delta receipts to the same dispatch/unit scope and summarize them through the ready gate.
inputs:
  - source_root: agents
  - proposal_profile: self-modify-patch-v1
  - session_mode: standard
expected:
  worker_dispatch_receipt_ok: true
  worker_delta_receipts_bound: true
  worker_delta_scan_profile: git-target-path-delta-v1
metric: equality
threshold: 1.0
ethics_check: true
